# Environment template for PromptForm
# Copy this file to ".env" and fill in real values. Do NOT commit the .env file.
# Secrets must live on the server. The frontend only reads VITE_* variables at build time.
GEMINI_API_KEY=your_gemini_api_key_here

# Base URL of your backend API used by the Vite client (only VITE_* vars are exposed)
VITE_API_BASE=http://localhost:3001

# Optional: override model version used by server (prevents 404 on retired versions)
GEMINI_MODEL=gemini-flash-latest
# Optional: limit characters of extracted document text sent to model
DOC_TEXT_CHAR_LIMIT=15000